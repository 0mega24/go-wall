version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true

run:
  timeout: 5m
